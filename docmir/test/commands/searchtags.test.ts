import {expect, test} from '@oclif/test'
/*
describe('searchtags', () => {
  test
  .stdout()
  .command(['searchtags'])
  .it('runs hello', ctx => {
    expect(ctx.stdout).to.contain('hello world')
  })

  test
  .stdout()
  .command(['searchtags', '--name', 'jeff'])
  .it('runs hello --name jeff', ctx => {
    expect(ctx.stdout).to.contain('hello jeff')
  })
})
*/

